###################################
# INGRESS
###################################
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: delivery-together-score-ingress
  labels:
    name: delivery-together-score-ingress
spec:
  rules:
  - http:
      paths:
      - pathType: Prefix
        path: /scores
        backend:
          service:
            name: delivery-together-score-service
            port: 
              number: 80